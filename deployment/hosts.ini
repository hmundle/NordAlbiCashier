[nac_servers]
nordalbi02 db_role=primary
nordalbi03 db_role=standby

[nac_servers:vars]
ansible_user=nordalbi
# Sudo password for system update privilege escalation
#ansible_become_password=Zeltstadt

# common configuration
nac_image_tag=3.0.0
aspnetcore_environment=Production

db_name=NacDB
db_user=postgres
db_password=NacP@ssw0rd

# primary_host is resolved dynamically in the playbook from db_role,
# using each host's inventory_hostname. Make sure nordalbi02/nordalbi03
# resolve for each other via DNS or /etc/hosts. It is used as the source
# host for the periodic pg_dump backup taken from host 2.

backup_dir=/var/backups/nac_db
backup_keep_count=30