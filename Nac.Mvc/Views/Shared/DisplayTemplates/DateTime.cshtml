@using Nac.Lib.Utilities;
@model DateTime?
@if (Model == null)
{
    @:Unknown
}
else
{
    if (ViewData.ModelMetadata.IsNullableValueType)
    {
        @:@(Model.Value.ToIsoUtcNoSubSec())
    }
    else
    {
        @:@(((DateTime)Model).ToIsoUtcNoSubSec())
    }
}
